package il.cshaifasweng.OCSFMediatorExample.client;

/**
 * Plain launcher class. It does NOT extend javafx.application.Application,
 * which lets the application start correctly from a shaded ("fat") jar where
 * JavaFX lives on the classpath rather than the module path.
 */
public class Main {

    public static void main(String[] args) {
        App.main(args);
    }
}
